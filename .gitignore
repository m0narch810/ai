# secrets — never commit
.env
.env.local
*.cookie

# captured data
data/raw/
data/scored/

# captured API samples (live account data)
fixtures/

# generated dashboard payload (live board data; deploy still uploads it from local)
web/dashboard.json
_board_preview.png

# node
node_modules/
dist/
*.log
.DS_Store
